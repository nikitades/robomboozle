{
  "name": "robomboozle-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "compile": "tsc server.ts --outDir dist --sourceMap",
    "start": "node dist/server.js",
    "build": "npm run clean && npm run compile",
    "debug": "npm run clean && npm run compile && npm run start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "@types/argparse": "^1.0.36",
    "@types/express": "^4.17.2",
    "@types/socket.io": "^2.1.4",
    "argparse": "^1.0.10",
    "express": "^4.17.3",
    "socket.io": "^2.3.0",
    "stream-split": "^1.1.0",
    "typescript": "^3.7.5"
  }
}
